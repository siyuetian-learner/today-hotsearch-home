# 今日热搜页面迭代对话记录

## 第 1 轮：基础结构

**用户：** 先做一个能跑的首页，顶栏显示站点名和介绍，主区域有微博、知乎、B站三个平台卡片，每个卡片放 Top 10。

**优化：** 使用 HTML 搭建顶栏、榜单摘要区和三列卡片网格；Mock 数据写入 `app.js`；每条热搜包含排名、标题、热度，标题链接先用 `#`。

## 第 2 轮：信息密度与视觉层级

**用户：** 页面要更像资讯站首页，不要太松散，参考今日头条的信息密度。

**优化：** 缩紧列表行高和卡片间距，加入平台色竖条、Top 10 标签、热度数字等视觉锚点；前三名排名突出，普通条目保持克制。

## 第 3 轮：响应式与细节交互

**用户：** 补齐移动端和交互状态，手机端一列展示，标题要可点击。

**优化：** 增加移动端断点，桌面三列、手机一列；列表 hover 时变色，标题 hover 使用品牌红；加入“刷新热度”按钮，模拟热度变化并自动重新排序。

## 第 4 轮：快速扫读焦点

**用户：** 榜单信息已经完整，但用户进入首页后还需要更快看到每个平台当前最热的一条。

**优化：** 在卡片网格上方新增“今日焦点”摘要条，自动提取微博、知乎、B站各自第一名，展示平台、热度和标题，让用户先扫重点，再进入完整 Top 10。

## 第 5 轮：关键词筛选

**用户：** 热搜条目多的时候，希望能按关键词快速过滤，比如输入“端午”“AI”“毕业”。

**优化：** 顶部工具栏加入搜索框，输入关键词后实时过滤三个榜单内的标题；无匹配时在对应卡片中显示空状态，同时保留刷新热度与重新排序能力。

## 第 6 轮：接入真实数据后端

**用户：** 按教程要求继续迭代，新增 Hugging Face、AI HOT、GitHub 这些网址的热搜。

**优化：** 新增 Express 后端，提供 `/api/hot` 与 `/api/hot/:source`，把 Hugging Face、AI HOT、GitHub 映射为统一榜单 JSON；保留微博、知乎、B站 Mock 数据作为已有平台展示。

## 第 7 轮：缓存、刷新与容错

**用户：** 搜索底层参考 AI HOT 与示例逻辑，真实数据源要稳定，不要因为一个平台失败导致整站不可用。

**优化：** 增加内存缓存，默认 600 秒；支持 `?refresh=1` 强制刷新；每个平台单独捕获错误，前端显示失败态与重试按钮。

## 第 8 轮：任务文件输出

**用户：** 最后按照任务要求输出任务文件。

**优化：** 新增 `RESEARCH.md`、`PRD.md`、`TECH_DESIGN.md`、`AGENTS.md`、`README.md`、`TASK_SUBMISSION.md`，覆盖需求研究、产品定义、技术设计、AI 开发指令、本地运行和打卡提交材料。
